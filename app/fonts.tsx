import { Klee_One, Aleo } from 'next/font/google';

export const kleeOne = Klee_One({
  weight: ['400', '600'],
  subsets: ['latin'],
});

export const aleo = Aleo({
  weight: ['400', '800'],
  subsets: ['latin'],
});
